# 115finance_engine: 高效能財務數據處理與自動化匯出引擎

## 📌 專案動機
身為具備會計背景的學生，在處理海量財務分錄時，我深刻感受到傳統辦公軟體（如 Excel）在面對非結構化數據與大規模運算時的效能瓶頸。為了徹底解決自動化對帳與數據分析的實務痛點，我決定跳脫「軟體使用者」的框架，以 **C 語言** 獨立開發這套高效能財務數據處理引擎。

本專案的核心目標不只是功能實作，更是對計算機科學底層邏輯的嚴謹挑戰：
- **挑戰效能極限**：捨棄高階語言的便利，選擇 C 語言以達成對記憶體的精確控制。透過實作 **$O(n \log n)$ 的Quick Sort**，確保系統在處理百萬級距的財務數據時，依然保有極致的運算效率。
- **實作動態擴容機制**：為了應對未知規模的數據流，我捨棄了靜態陣列，自主實作了**動態記憶體擴張邏輯**。透過 `realloc` 策略優化空間利用率，展現對資料結構與記憶體管理的深度思考。
- **跨領域知識融合**：利用對會計科目與財務邏輯的理解，將專業知識轉化為程式邏輯，實現了具備「互動式選單」與「多維度排序」的解析引擎。

## 🚀 核心功能
- **互動式終端機介面**：提供使用者友善的選單系統，可即時切換不同的數據處理與檢視模式。
- **多維度高效能排序**：實作多個 Comparator Functions，結合時間複雜度 $O(n \log n)$ 的 `qsort` 演算法，允許使用者按「金額」、「日期」或「會計科目」動態重新排列數據。
- **CSV 自動化解析**：讀取標準財務數據格式，將非結構化文本精準轉為高效的 `struct` 結構體。
- **資料持久化匯出**：具備自動化匯出功能，可將處理後的數據重新封裝並輸出為標準 CSV 格式，實現與外部報表系統（如 Excel）的無縫對接。
- **動態記憶體管理**：透過 `malloc` 與 `free` 在 Heap 區動態配置資源，確保在大規模運算下依然保有高穩定性與零洩漏（Zero Leak）。

## 🛠️ 技術細節
- **開發語言**：C11
- **核心資料結構**：Structured Arrays, Dynamic Memory (Heap)
- **進階指標應用**：使用 **Function Pointers** 驅動多重排序邏輯。
- **Amortized Time 分析**：採用翻倍擴張策略，使資料插入的時間複雜度在平攤後仍保持極高效的 **$O(1)$ Amortized Time**。

## 📂 檔案說明
- `finance_engine.c`: 專案主程式原始碼。
- `data.csv`: 測試數據檔案（包含模擬日常開銷的紀錄）。
- `sorted_export.csv`: (執行選單選項 4 後產生) 經由引擎處理並匯出的結構化數據。

## 📝 技術筆記：數據流處理原理
本專案原生實作了檔案系統的輸入與輸出：
1. **數據讀取**：利用 `fopen` 建立檔案指標，透過 `fgets` 逐行讀取文本，避免一次性載入過大檔案導致記憶體溢位，並由 `strtok` 進行數據型態轉換。
2. **數據匯出**：實作 `exportToCSV` 函數，利用 `fprintf` 格式化輸出，精確控制定界符與浮點數精度，確保匯出檔案符合工業標準格式。

## 📈 執行方式
1. 將 `data.csv` 置於與執行檔相同的路徑下。
2. 在終端機編譯：
   ```bash
   gcc finance_engine.c -o finance_engine
   ./finance_engine

## ✅ 執行截圖
### 1. 互動式選單與數據預覽
<img width="1389" height="928" alt="image" src="https://github.com/user-attachments/assets/0e44f1be-d7b9-41a6-8fc5-fe1ca0e3805b" />
### 2. 執行選單選項 4 後匯出至 Excel (CSV 格式)：
<img width="1919" height="1133" alt="image" src="https://github.com/user-attachments/assets/50a82894-d7e3-4a89-ae99-26bc005c0488" />



